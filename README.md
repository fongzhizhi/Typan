# Typan

> 专为 TypeScript 设计的、健壮且类型安全的实用工具集，用于增强原生 JavaScript 对象。

[![npm version](https://img.shields.io/npm/v/typan.svg)](https://www.npmjs.com/package/typan)
[![license](https://img.shields.io/npm/l/typan.svg)](./LICENSE)

**Typan** (意为 Type + Titan，即“类型泰坦”) 是一个为现代 TypeScript 开发精心构建的工具库。它提供了一系列健壮、类型安全的辅助函数，用以封装和增强原生 JavaScript 对象（如 `Array`、`Object`、`Map`、`Number`、`Date` 等），使你的代码更安全、更具表达力、更简洁。

## ✨ 特性

*   **🛡️ 类型安全为先**：每个工具都基于严格的 TypeScript 类型构建，让你拥有充分的信心和卓越的 IDE 智能提示。
*   **🧰 增强原生 API**：解决原生 JavaScript 对象方法的常见痛点并填补其功能空白（例如安全遍历、严格验证）。
*   **⚡ 零依赖**：保持你的项目轻量且安全。
*   **📦 支持 Tree Shaking**：以 ES 模块构建，确保你的打包产物只包含你实际使用的部分。
*   **🔧 实用主义设计**：专注于真实世界的用例，提供清晰、可预测的 API。

## 📦 安装

```bash
npm install typan
# 或
yarn add typan
# 或
pnpm add typan
```

## 🚀 快速开始

```typescript
import { iterateArr, isValidNumber, isNone } from 'typan';

// 1. 安全、可中断的数组遍历
const numbers = [1, 2, 3, 4];
let found = false;
iterateArr(numbers, (num, index) => {
  if (num > 2) {
    found = true;
    return true; // 中断遍历，类似于 `some()` 方法
  }
});

// 2. 严格的数字验证
isValidNumber(42);        // true
isValidNumber(NaN);       // false
isValidNumber(Infinity);  // false

// 3. 智能的空值（undefined、null、空字符串）检查
isNone(null);           // true
isNone('');             // true
isNone(undefined);      // true
isNone(0);              // false
isNone(false);          // false
```

## 📚 核心工具

### 数组 (`iterateArr`, `safeFind`, `chunk`, ...)
以内置的安全性和更好的控制力执行常见操作。
```typescript
import { chunk } from 'typan';
chunk([1, 2, 3, 4, 5], 2); // 返回 [[1, 2], [3, 4], [5]]
```

### 对象 (`iterateObject`, `safeGet`, `deepMerge`, ...)
安全地访问、遍历和操作对象，避免意外错误。
```typescript
import { safeGet } from 'typan';
const obj = { a: { b: { c: 42 } } };
safeGet(obj, 'a.b.c'); // 返回 42
safeGet(obj, 'a.b.d', 'default'); // 返回 'default'
```

### 数字 (`isValidNumber`, `isNumeric`, `clamp`, ...)
超越 `typeof`，提供智能处理 `NaN`、`Infinity` 和数字字符串的工具。

### Map & Set (`keyArray`, `valueArray`, `filterMap`, ...)
为现代 JavaScript 集合提供具有完整类型支持的扩展工具。

### 日期 (`isValidDate`, `format`, `addDays`, ...)
提供可靠的日期解析、验证和计算。

*...（你可以在后续迭代中不断完善此列表）...*

## 🧩 设计哲学

Typan 诞生于一个简单的想法：**日常开发应该更加顺畅**。它并不试图取代 lodash 或提供所有可能的函数。相反，它专注于：

1.  **安全性**：防范 `undefined`、`null`、`NaN` 和意外的类型。
2.  **清晰性**：提供直观的函数名和可预测的行为。
3.  **实用性**：解决 TypeScript 项目中 80% 时间里出现的 20% 的核心问题。

## 🤝 贡献

欢迎提交贡献、问题或功能请求！请随时查看 [议题页面](https://github.com/你的GitHub用户名/Typan/issues)。

## 📄 许可证

本项目基于 **MIT 许可证** - 详见 [LICENSE](./LICENSE) 文件。

---
**Typan** – TypeScript 工具领域的泰坦。